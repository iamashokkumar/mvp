{
	"role":{
		"name": "user",
		"schema_privileges": [{
		   "privileges": ["SELECT METADATA", "SELECT CDS METADATA", "SELECT", "INSERT", "UPDATE", "EXECUTE", "DELETE", "DROP", "CREATE TEMPORARY TABLE" ]
		}]
	}
}